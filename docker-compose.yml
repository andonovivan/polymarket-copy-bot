services:
  bot:
    build: .
    env_file: .env
    restart: unless-stopped
    ports:
      - "${DASHBOARD_PORT:-8080}:${DASHBOARD_PORT:-8080}"
    environment:
      - BOT_DB_PATH=/app/state/bot_state.db
    volumes:
      - bot_state:/app/state

volumes:
  bot_state:
